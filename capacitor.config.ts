import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nivedhLabs.WorldBordersnCountryInfoApp',
  appName: 'World Atlas & Country Info',
  webDir: 'dist'
};

export default config;
